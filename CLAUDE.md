# GD AI Level Generator

Geometry Dash 레벨을 AI가 생성하는 도구를 만드는 프로젝트.

## 최종 목표

음악을 입력하면 그에 맞는 플레이 가능한 GD 레벨을 생성한다.

## 큰 그림 (3단계 파이프라인)

### 1. 음악 분석
- BPM, 비트 위치, 섹션, 드롭, 에너지 곡선 추출
- 다음 단계에 "타이밍 지도" 전달
- 도구 후보: librosa

### 2. 레이아웃
- 음악 정보 받아서 게임플레이 뼈대 구성
- 게임플레이 오브젝트 (블록, 스파이크, 점프 패드 등)
- 게임플레이 트리거 (스피드 포털, 모드 전환, 중력 반전 등)
- 한 번에 통째로 X → 계층적으로: 큰 뼈대 → 구간 → 디테일

### 3. 디자인
- 레이아웃 위에 시각 요소 입히기
- **블록 디자인** (게임플레이 블록 외형)
- **색상** (채널 분배, 팔레트)
- **시각 트리거** (Color/Pulse, Move/Rotate 장식, 카메라, 셰이더)
- 캐릭터/배경 그림은 한참 뒤 옵션

## 핵심 원칙

- **레이아웃 먼저, 디자인 나중** — 실제 크리에이터 워크플로우 따라감
- **한 번에 다 만들기 X** — 단계별로 쪼개서 점진적 생성
- **AI는 플레이 못 함** — 정량 검증(점프 거리)은 가능, 플레이감은 데이터로 간접 학습
- **MVP부터** — 처음엔 단순 큐브 레벨, 점진적으로 복잡도 추가

## 데이터 형식

### .gmd 파일 구조
```
.gmd (XML 평문)
├── k1: 레벨 ID
├── k2: 레벨 이름
├── k5: 만든 사람
└── k4: 인코딩된 레벨 데이터
       └── Base64 + Gzip 풀면:
           ├── 헤더 (kS38: 색상 채널)
           ├── 레벨 설정 (kA13, kA15...)
           └── 오브젝트 리스트 (1,1,2,255,3,15,...;)
```

### 오브젝트 형식
- `,`로 키-값 쌍 구분, `;`로 오브젝트 구분
- 핵심 키: 1=ID, 2=X, 3=Y, 6=회전

### CCLocalLevels.dat (참고용)
- 모든 로컬 레벨이 한 파일에
- .gmd와 거의 같은 구조 + XOR(키 11) 한 겹 더

## 작업 진행 상황

### ✅ 완료
- .gmd 파일 디코딩 (Base64 + Gzip)
- 오브젝트 파싱 (헤더/설정/오브젝트 분리)
- 메타데이터 추출 (이름, ID, 만든 사람)
- gmdkit으로 ID/키 매핑 확보
  - 일반 오브젝트 키 50개
  - 트리거 ID 80개
  - 트리거별 키 매핑 (Move 27개 등)

### ⏳ 진행 중
- 게임 리소스 분석 (집 PC 필요)

### 📋 해야 할 일

**집 가서 즉시:**
1. `CCLocalLevels.dat` 백업
2. `Resources/` 폴더에서 `GJ_GameSheet.png`, `GJ_GameSheet.plist` 복사
3. GDBrowser에서 ID 99784974 ("Object IDs New") 받아서 .gmd로 export
4. (선택) Geode SDK 설치

**그다음:**
- 인코딩 코드 (역방향: 데이터 → .gmd)
- 왕복 테스트 (파싱 → 인코딩 정합성)
- 데이터셋 더 모으기 (수백~수천 레벨)
- 모든 ID 사전 만들기 (99784974 레벨 파싱)
- 히트박스 데이터 추출

**나중에:**
- 음악 분석 모듈
- 학습 모델 또는 에이전트 시스템
- 플레이 가능성 검증기
- 시각화 도구 (텍스처 추출 후)

## 우리가 못 가진 정보 (게임 코드/리소스에 있음)

- 시각 정보 (오브젝트 텍스처)
- 히트박스 (크기, 모양, 종류)
- 모든 ID 의미 (4539개)
- 트리거 값 의미 (예: EASING 5번이 뭔지)
- 물리 상수 (점프 높이, 속도, 중력)

→ 게임 뜯어보면 한 번에 다 풀림

## 환경

- Python 3
- 핵심 라이브러리: `gmdkit` (pip install)
- 향후: librosa (음악), PyTorch (학습), Pillow (시각화)

## 코드 파일

- `decode_gmd.py` — .gmd 파일 디코딩
- `parse_objects.py` — 오브젝트 파싱

## 참고 자료

- gmdkit (PyPI): 데이터 구조 라이브러리
- GDBrowser (gdbrowser.com): 레벨 검색/다운로드
- Library of Geometria (a-zalt.github.io/gdknowledge): ID 잡학사전
- Geometry Dash Wiki (geometrydash.wiki.gg): 트리거/오브젝트 설명
- Geode SDK (geode-sdk.org): 게임 모드 도구
- ID 99784974 (gdbrowser.com): 모든 오브젝트 ID 정리 레벨
